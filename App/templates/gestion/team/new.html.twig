{% extends 'base.html.twig' %}

{% block body %}
    <div class="container mx-auto p-4 pt-6 md:p-6 lg:p-12">
        <h1 class="text-4xl font-bold mb-4">Créer une nouvelle équipe</h1>

        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="bg-orange-500 hover:bg-orange-700 text-white font-bold py-2 px-4 rounded">Créer</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
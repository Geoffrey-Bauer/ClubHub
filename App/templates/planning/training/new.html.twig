{% extends 'base.html.twig' %}

{% block title %}Nouvel entraînement - ClubHub{% endblock %}

{% block body %}
    <div class="min-h-screen bg-gray-100">
        {% include '_templates/_nav.html.twig' %}

        <div class="py-10">
            <header>
                <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
                    <h1 class="text-3xl font-bold leading-tight text-gray-900">Nouvel entraînement</h1>
                </div>
            </header>
            <main>
                <div class="max-w-3xl mx-auto sm:px-6 lg:px-8">
                    <div class="mt-8 bg-white shadow-lg rounded-lg overflow-hidden">
                        <div class="px-4 py-5 sm:p-6">
                            {% if form_errors(form) %}
                                <div class="mb-4 p-4 bg-red-100 border border-red-400 text-red-700 rounded-md">
                                    {{ form_errors(form) }}
                                </div>
                            {% endif %}

                            {{ form_start(form, {'attr': {'class': 'space-y-6'}}) }}
                            <div>
                                {{ form_label(form.date, null, {'label_attr': {'class': 'block text-sm font-medium text-gray-700 mb-1'}}) }}
                                <div class="mt-1">
                                    {{ form_widget(form.date, {'attr': {
                                        'class': 'block w-full px-4 py-3 text-base border-2 border-gray-300 rounded-md focus:ring-indigo-500 focus:border-indigo-500 bg-white shadow-sm',
                                        'placeholder': 'Sélectionnez la date et l\'heure'
                                    }}) }}
                                </div>
                            </div>

                            <div>
                                {{ form_label(form.team, null, {'label_attr': {'class': 'block text-sm font-medium text-gray-700 mb-1'}}) }}
                                <div class="mt-1">
                                    {{ form_widget(form.team, {'attr': {
                                        'class': 'block w-full px-4 py-3 text-base border-2 border-gray-300 rounded-md focus:ring-indigo-500 focus:border-indigo-500 bg-white shadow-sm'
                                    }}) }}
                                </div>
                            </div>

                            <div class="pt-5 border-t border-gray-200">
                                <div class="flex justify-end">
                                    <a href="{{ path('planning_training_list') }}" class="bg-white py-2 px-4 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 transition-colors duration-150">
                                        Annuler
                                    </a>
                                    <button type="submit" class="ml-3 inline-flex justify-center py-2 px-4 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 transition-colors duration-150">
                                        Enregistrer l'entraînement
                                    </button>
                                </div>
                            </div>
                            {{ form_end(form) }}
                        </div>
                    </div>
                </div>
            </main>
        </div>
    </div>
{% endblock %}